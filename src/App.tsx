// App solo sirve como contenedor mínimo si no usamos Router.
// En este proyecto, el enrutamiento está en `src/router.tsx`.
export default function App() {
  return (
    <div>
      App base
    </div>
  )
}
